[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"

# Netlify automatically installs its current Next.js adapter.
# Runtime secrets and AWS_LAMBDA_JS_RUNTIME=nodejs24.x belong in the Netlify UI.
