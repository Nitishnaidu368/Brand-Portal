# Where the SQLite database and uploaded files live (default: ./data)
DATA_DIR=./data

# Largest single upload accepted by the admin uploader, in megabytes
MAX_UPLOAD_MB=50

# Set to 1 only if you run `next start` over plain http on a non-localhost host
INSECURE_COOKIES=0
